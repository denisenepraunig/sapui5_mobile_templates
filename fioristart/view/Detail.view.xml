<core:View
	controllerName="com.nepraunig.fioristart.view.Detail"
	xmlns="sap.m"
	xmlns:core="sap.ui.core" >
	<Page
		title="{i18n>EmptyTitle}"
		showNavButton="true"
		navButtonPress="handleNavButtonPress" >
		<VBox>
			<Text text="{SoId}" />
			<Text text="{GrossAmount}" />
			<Text text="{CreatedAt}" />
			<Text text="{CreatedByBp}" />
		</VBox>
	</Page>
</core:View>